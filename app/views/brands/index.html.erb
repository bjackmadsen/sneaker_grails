<div class="jumbotron">
<h1>SNEAKER</h1>
<h1>GRAILS</h1>
</div>

<ul>
	<% @brands.each do |brand| %>
		<li><%= link_to brand.name, brand_path(brand) %></li>
	<% end %>
</ul>

<%= link_to "Add a grail", new_style_path, :class => "btn btn-info btn" %>
<%= link_to "Add a brand", new_brand_path, :class => "btn btn-success btn" %>
